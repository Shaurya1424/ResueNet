# NOTE: values below are base64-encoded examples.
# In production, use sealed-secrets, external-secrets, or
# `kubectl create secret generic rescuenet-secret --from-literal=JWT_SECRET=...`
apiVersion: v1
kind: Secret
metadata:
  name: rescuenet-secret
  namespace: rescuenet
type: Opaque
data:
  # echo -n "supersecretjwtkey" | base64
  JWT_SECRET: c3VwZXJzZWNyZXRqd3RrZXk=
